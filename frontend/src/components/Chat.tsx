import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui";

export function Chat() {
  return (
    <Card className="h-1/2">
      <CardHeader>
        <CardTitle>Chat</CardTitle>
      </CardHeader>
      <CardContent>Ping!</CardContent>
    </Card>
  );
}
